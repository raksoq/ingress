apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pikaflask.fullname" . }}-test-connection"
  labels:
    {{- include "pikaflask.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pikaflask.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
